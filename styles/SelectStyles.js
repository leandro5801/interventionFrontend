// SelectStyles.js
export const customStyles = {
    control: (base) => ({
      ...base,
      backgroundColor: '#ccc',
    }),
    dropdownIndicator: base => ({
      ...base,
      color: "gray" // Custom colour
    })
  };
  